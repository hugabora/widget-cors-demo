[project]
name = "widget-backend"
version = "1.0.0"
description = "FastAPI backend for widget CORS demo"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.116.1",
    "uvicorn[standard]==0.35.0",
]

[dependency-groups]
dev = [
    "ruff==0.6.9",
]
